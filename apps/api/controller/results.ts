import {Request,Response} from "express"
export const results=(req:Request,response:Response)=>{
const {modelId,results}=req.body;
// check if model is presnt in the db or not.
// also set slot to free.


}